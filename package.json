{
  "name": "ts-prototype",
  "version": "1.0.0",
  "description": "simple web-project with typescript",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "start": "npm run clean && npm run lint && npm run build && node build/app.js"
  },
  "author": "Fedor Rusak",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.10",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  }
}
